import { locales } from '../utils'

export default {
  baseUrl: 'https://enalfarid.digital',
  seo: false,
  defaultLocale: 'id',
  detectBrowserLanguage: false,
  lazy: true,
  langDir: 'locales/',
  locales
}
